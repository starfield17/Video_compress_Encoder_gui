[tool.ruff]
target-version = "py312"
line-length = 120
exclude = ["FFmpeg", "build", "dist", "workdir"]

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F"]

[tool.pyright]
pythonVersion = "3.12"
typeCheckingMode = "basic"
include = ["core", "cli"]
exclude = ["build", "dist", "workdir"]
